-- Поріг «закінчується» переїжджає з товару на категорію: сигнал потрібен
-- тоді, коли закінчується ПОТРЕБА (зубні щітки), а не конкретна марка.
alter table public.categories
  add column threshold numeric not null default 1 check (threshold >= 0);

-- items.threshold лишається як запасний поріг для товарів поза категоріями,
-- але з форми прибраний: керувати ним звідти більше не треба.
